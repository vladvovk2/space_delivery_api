<% if @cart.line_items.empty? %>

<div class='empty-cart'>
  <h3> Cart is empty</h3>
</div>

<% else %>

<div id='cart'>
  <%= render @cart %>
</div>

<div id='buy_together'>
  <% @products.each do |product| %>
    <%= product.title %>
  <% end %>
</div>
<% end %>