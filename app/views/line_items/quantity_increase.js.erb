$('#cart').on('ajax:success').html("<%= j render(current_cart) %>");

$("#cart-icon").on('ajax:success').html('<%= current_cart.total_price %> UA');
$("#cart-total-price").on('ajax:success').html('<%= current_cart.total_price %> UA');