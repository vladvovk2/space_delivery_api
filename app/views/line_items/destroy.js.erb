$('[class*="line-item"]').bind('ajax:success', function(){
  var lineItem = $(this);

  lineItem.animate({opacity: 0}, 100, function() {
      lineItem.animate({height: 0, margin: 0}, 100);
  });
});

$("#cart-icon").html('<%=  number_to_currency current_cart.total_price %>');
$("#cart-total-price").html('<%=  number_to_currency current_cart.total_price %>');
